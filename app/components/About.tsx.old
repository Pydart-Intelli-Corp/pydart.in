"use client";

import { useEffect, useRef, useState } from 'react';
import Image from 'next/image';
import Link from 'next/link';
import { motion, useAnimation, useInView } from 'framer-motion';

// Image rotation helper
const useImageRotation = (images: string[], interval: number = 8000) => {
  const [currentIndex, setCurrentIndex] = useState(0);
  const [isTransitioning, setIsTransitioning] = useState(false);
  
  useEffect(() => {
    const timer = setInterval(() => {
      setIsTransitioning(true);
      setTimeout(() => {
        setCurrentIndex((prevIndex) => (prevIndex + 1) % images.length);
        setIsTransitioning(false);
      }, 800); // fade duration
    }, interval);
    
    return () => clearInterval(timer);
  }, [images.length, interval]);
  
  return { currentImage: images[currentIndex], isTransitioning };
};

export default function About() {
  // Animation controls
  const controls = useAnimation();
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true, margin: "-100px 0px" });
  
  // Company values inspired by mission.dart
  const values = [
    {
      id: 'excellence',
      icon: '/assets/icons/excellence.svg',
      title: 'Excellence',
      description: 'We relentlessly pursue perfection in everything we do, from breakthrough engineering to meticulous client service, establishing benchmarks that elevate entire industries.',
      color: 'bg-[#FF4D00]',
      lightColor: 'bg-[#FF4D00]/10',
      textColor: 'text-[#FF4D00]'
    },
    {
      id: 'innovation',
      icon: '/assets/icons/innovation.svg',
      title: 'Innovation',
      description: 'We challenge conventional thinking and embrace calculated risks to develop breakthrough solutions that create unprecedented value and open new frontiers for our clients.',
      color: 'bg-[#FF4D00]',
      lightColor: 'bg-[#FF4D00]/10',
      textColor: 'text-[#FF4D00]'
    },
    {
      id: 'integrity',
      icon: '/assets/icons/integrity.svg',
      title: 'Integrity',
      description: 'We operate with unwavering ethical standards, ensuring every business decision reflects our commitment to honesty, accountability, and long-term trusted partnerships.',
      color: 'bg-[#FF4D00]',
      lightColor: 'bg-[#FF4D00]/10',
      textColor: 'text-[#FF4D00]'
    }
  ];
  
  // Team member data
  const team = [
    {
      name: "Tishnu Thankappan",
      position: "Founder / CEO",
      image: "/assets/images/team/tishnu.jpg",
      quote: "With a strong background in research and development in embedded systems, as well as expertise in Flutter and .NET."
    },
    {
      name: "Adwaith Raj PR",
      position: "Co-Founder / CTO",
      image: "/assets/images/team/adwaith.jpg",
      quote: "Expertise in cloud technologies and a keen understanding of the latest industry trends."
    },
    {
      name: "Akshara Ravi",
      position: "Co-Founder / CMO",
      image: "/assets/images/team/akshara.jpg",
      quote: "Transforming brands with agile digital strategies and data-driven innovation."
    }
  ];
  
  // Stats data
  const stats = [
    { value: "5+", label: "AI Solutions" },
    { value: "3+", label: "Industries Served" },
    { value: "100%", label: "Client Satisfaction" },
    { value: "24/7", label: "Support" }
  ];
  
  const [selectedTeamMember, setSelectedTeamMember] = useState(0);
  
  // Start animation when in view
  useEffect(() => {
    if (isInView) {
      controls.start("visible");
    }
  }, [controls, isInView]);
  
  return (
    <section id="about" className="relative overflow-hidden">
      {/* Modern about section with split design */}
      <div className="bg-gradient-to-br from-gray-50 to-white py-24">
        {/* Decorative elements */}
        <div className="absolute top-0 right-0 w-1/3 h-1/3 bg-gradient-to-br from-[#FF4D00]/5 to-transparent rounded-full blur-3xl"></div>
        <div className="absolute bottom-0 left-0 w-1/4 h-1/4 bg-gradient-to-tr from-[#FF4D00]/5 to-transparent rounded-full blur-3xl"></div>
        
        <div ref={ref} className="container mx-auto px-4 sm:px-6 lg:px-8">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
            {/* Left column - About content */}
            <motion.div 
              initial={{ opacity: 0, x: -30 }}
              animate={controls}
              variants={{
                visible: { opacity: 1, x: 0, transition: { duration: 0.8, ease: "easeOut" } },
              }}
              className="space-y-8"
          >
            <div className="mb-8">
              <h4 className="text-blue-600 font-semibold tracking-widest text-sm mb-4">
                ABOUT PYDART INTELLI CORP
              </h4>
              
              <h2 className="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 mb-6">
                AI-Powered Innovation for Business Growth
              </h2>
            </div>
            
            <h4 className="text-blue-600 font-semibold tracking-widest text-sm mb-4">
              OUR MISSION
            </h4>
            
            <p className="text-xl md:text-2xl lg:text-3xl text-gray-800 font-medium leading-relaxed max-w-4xl mx-auto">
              To drive transformative growth for our clients by delivering innovative AI-powered solutions that bridge present limitations and future possibilities, creating enduring value for businesses throughout the Indian market.
            </p>
            
            {/* Stats display */}
            <div className="grid grid-cols-2 md:grid-cols-4 gap-8 mt-16 max-w-4xl mx-auto">
              {stats.map((stat, index) => (
                <motion.div 
                  key={index}
                  initial={{ y: 20, opacity: 0 }}
                  animate={controls}
                  variants={{
                    visible: { 
                      y: 0, 
                      opacity: 1,
                      transition: { 
                        duration: 0.5, 
                        ease: "easeOut",
                        delay: 0.1 * index 
                      }
                    },
                  }}
                  className="text-center"
                >
                  <h3 className="text-3xl md:text-4xl font-bold text-gray-900 mb-2">{stat.value}</h3>
                  <p className="text-gray-600">{stat.label}</p>
                </motion.div>
              ))}
            </div>
          </motion.div>
        </div>
      </div>
      
      {/* Core values section - inspired by mission.dart */}
      <div className="bg-gray-50 py-20">
        <div className="container mx-auto px-4 sm:px-6 lg:px-8">
          <div className="max-w-6xl mx-auto">
            <motion.div
              initial={{ y: 30, opacity: 0 }}
              whileInView={{ y: 0, opacity: 1 }}
              viewport={{ once: true, margin: "-100px 0px" }}
              transition={{ duration: 0.8 }}
              className="mb-16"
            >
              <h4 className="text-blue-600 font-semibold tracking-widest text-sm mb-4">
                CORE VALUES
              </h4>
              
              <h2 className="text-3xl md:text-4xl font-bold text-gray-900">
                The Principles That Guide Us
              </h2>
            </motion.div>
            
            <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
              {values.map((value, index) => (
                <motion.div
                  key={value.id}
                  initial={{ y: 40, opacity: 0 }}
                  whileInView={{ y: 0, opacity: 1 }}
                  viewport={{ once: true, margin: "-50px 0px" }}
                  transition={{ duration: 0.6, delay: index * 0.2 }}
                  className="bg-white rounded-lg shadow-xl overflow-hidden"
                >
                  <div className={`${value.color} h-2`}></div>
                  <div className="p-8">
                    <div className={`w-12 h-12 ${value.lightColor} rounded-lg flex items-center justify-center mb-6`}>
                      <span className={`material-icons ${value.textColor} text-2xl`}>
                        {value.id === 'excellence' ? 'star' : value.id === 'innovation' ? 'lightbulb' : 'security'}
                      </span>
                    </div>
                    
                    <h3 className="text-xl font-bold mb-1 text-gray-900">{value.title}</h3>
                    
                    <p className="text-gray-600 leading-relaxed">
                      {value.description}
                    </p>
                  </div>
                </motion.div>
              ))}
            </div>
          </div>
        </div>
      </div>
      
      {/* Team section with interactive elements */}
      <div className="bg-white py-20">
        <div className="container mx-auto px-4 sm:px-6 lg:px-8">
          <div className="max-w-6xl mx-auto">
            <motion.div
              initial={{ y: 30, opacity: 0 }}
              whileInView={{ y: 0, opacity: 1 }}
              viewport={{ once: true, margin: "-100px 0px" }}
              transition={{ duration: 0.8 }}
              className="mb-16 text-center"
            >
              <h4 className="text-blue-600 font-semibold tracking-widest text-sm mb-4">
                OUR TEAM
              </h4>
              
              <h2 className="text-3xl md:text-4xl font-bold text-gray-900">
                The Experts Behind Our Success
              </h2>
            </motion.div>
            
            <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
              {/* Team member image */}
              <motion.div 
                initial={{ opacity: 0, scale: 0.95 }}
                whileInView={{ opacity: 1, scale: 1 }}
                viewport={{ once: true }}
                transition={{ duration: 0.8 }}
                className="relative"
              >
                <div className="relative rounded-lg overflow-hidden shadow-2xl aspect-w-4 aspect-h-3">
                  <Image 
                    src={team[selectedTeamMember].image} 
                    alt={team[selectedTeamMember].name} 
                    fill
                    className="object-cover"
                  />
                  <div className="absolute inset-0 bg-gradient-to-t from-black/70 via-transparent to-transparent"></div>
                  
                  <div className="absolute bottom-0 left-0 p-6 md:p-8">
                    <h3 className="text-xl md:text-2xl font-bold text-white mb-1">
                      {team[selectedTeamMember].name}
                    </h3>
                    <p className="text-blue-200 font-medium">
                      {team[selectedTeamMember].position}
                    </p>
                  </div>
                </div>
                
                {/* Decorative elements */}
                <div className="absolute -top-4 -right-4 w-20 h-20 bg-blue-600 rounded-full opacity-20"></div>
                <div className="absolute -bottom-6 -left-6 w-16 h-16 bg-blue-600 rounded-full opacity-30"></div>
              </motion.div>
              
              {/* Team member quote and navigation */}
              <motion.div
                initial={{ x: 30, opacity: 0 }}
                whileInView={{ x: 0, opacity: 1 }}
                viewport={{ once: true }}
                transition={{ duration: 0.8 }}
                className="space-y-8"
              >
                <blockquote className="text-2xl md:text-3xl font-medium text-gray-800 leading-relaxed border-l-4 border-blue-600 pl-6">
                  "{team[selectedTeamMember].quote}"
                </blockquote>
                
                <div className="flex items-center space-x-4">
                  {team.map((member, index) => (
                    <button
                      key={index}
                      onClick={() => setSelectedTeamMember(index)}
                      className={`w-10 h-10 rounded-full transition-all duration-300 flex items-center justify-center ${selectedTeamMember === index ? 'bg-blue-600 text-white' : 'bg-gray-200 text-gray-600 hover:bg-gray-300'}`}
                    >
                      {index + 1}
                    </button>
                  ))}
                </div>
              </motion.div>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
